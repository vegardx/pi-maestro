// @vegardx/pi-prompt-assist — Ghost-text suggestions, system-addendum nudges, gated input transforms.
// Scaffold placeholder extension entry. Replaced with a defineExtension
// factory (from @vegardx/pi-core) when this extension is implemented.
export default function activate(): void {
	// no-op until the prompt-assist extension lands
}
